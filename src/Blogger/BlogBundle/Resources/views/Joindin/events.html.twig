{% extends "::base.html.twig" %}

{% block title %}BloggerBlogBundle:Joindin:events{% endblock %}

{% block body %}
    {% for event in events %}
        {#<h1><a href="{{ path('rs_joindin_event', {index : event.index}) }}">{{ event.name }}</a></h1>#}
        <h1>{{ event.name }}</h1>
        <p>{{ event.description }}</p>
        <hr>
    {% endfor %}
{% endblock %}
