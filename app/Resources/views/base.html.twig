<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" />
        <title>{% block title %}Rate a book{% endblock %}</title>
        {% block stylesheets %}
            {% stylesheets
                'https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css'
                'https://www.w3schools.com/w3css/4/w3.css'
                '../app/Resources/public/css/screen.css' %}
        <link href="{{ asset_url }}" type="text/css" rel="stylesheet"/>
        {% endstylesheets %}
        {% endblock %}


        {% block javascript %}
            {% javascripts
                'https://ajax.googleapis.com/ajax/libs/jquery/3.3.1/jquery.min.js'
                'https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js'
            %}
            <script type="text/javascript" src="{{ asset_url }}"></script>
            {% endjavascripts %}
        {% endblock %}

        <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}" />
    </head>
    <body>
    <div class="container">
        {#<nav class="navbar navbar-default">#}
            {#<div class="container-fluid">#}
                {#<button type="button" class="navbar-toggle collapsed"#}
                        {#data-toggle="collapse"#}
                        {#data-target="#bs-example-navbar-collapse-1">#}
                    {#<span class="sr-only">Toggle navigation</span>#}
                    {#<span class="icon-bar"></span>#}
                    {#12#}
                    {#Advanced Web Development Symfony Configuration and Templating#}
                    {#<span class="icon-bar"></span>#}
                    {#<span class="icon-bar"></span>#}
                {#</button>#}
                {#<a class="navbar-brand" href="#">Rate a book</a>#}
                {#<div class="collapse navbar-collapse">#}
                    {#<ul class="nav navbar-nav navbar-right">#}
                        {#<li><a href="{{ path('index', {'page':1}) }}">Home</a></li>#}
                        {#{% if not is_granted("ROLE_USER") %}#}


                        {#{% else %}#}
                            {#<li><a href="{{ path('blogger_author') }}">My posts</a></li>#}
                            {#<li><a href="{{ path('blogger_book_create') }}">Add book</a></li>#}
                            {#<li><a href="{{ path('user_logout') }}">Logout</a></li>#}
                            {#<li><a href="{{ path('blogger_api_NYT_book_list') }}">Best sellers</a></li>#}
                            {#<li><a href="#">Logged in as {{ app.user.username }}</a></li>#}
                        {#{% endif %}#}


                    {#</ul> </div>#}
            {#</div>#}
        {#</nav>#}

        <nav class="navbar navbar-default">
            <div class="container-fluid">
                <div class="navbar-header">
                    <a class="navbar-brand" href="{{ path('index', {'page':1}) }}">Rate a book</a>
                </div>
                <ul class="nav navbar-nav">
                    <li><a href="{{ path('index', {'page':1}) }}">Home</a></li>
                    <li class="dropdown"><a class="dropdown-toggle" data-toggle="dropdown" href="#">Find books<span class="caret"></span></a>
                        <ul class="dropdown-menu">
                            <li><a href="{{ path('book_search') }}">Search for a book within the site</a></li>
                            <li><a href="{{ path('blogger_api_search') }}">Search for a book in the google library</a></li>
                            <li><a href="{{ path('blogger_api_NYT_book_list') }}">View Best sellers (according to the NYT)</a></li>
                            <li><a href="{{ path('rs_joindin_events_all') }}">View Events(workshop)</a></li>
                        </ul>
                    </li>
                {% if is_granted("ROLE_USER") %}
                    <li><a href="{{ path('blogger_author') }}">My posts</a></li>
                    <li><a href="{{ path('blogger_book_create') }}">Add book</a></li>
                </ul>
                <ul class="nav navbar-nav navbar-right">
                    <li><a href="#">Logged in as {{ app.user.username }}</a></li>
                    <li><a href="{{ path('user_logout') }}">Logout</a></li>
                </ul>

                {% else %}
                <ul class="nav navbar-nav navbar-right">
                    <li><a href="{{ path('fos_user_registration_register') }}">Sign Up</a></li>
                    <li><a href="{{ path('user_login') }}">Login</a></li>
                </ul>
                {% endif %}
            </div>
        </nav>

        <div class="container">
                {% block body %}{% endblock %}

        </div>
        <br>
        <div class="footer">
            {% block footer %}
                <p>Rate a book</p>
            {% endblock %} </div>
    </div>
    </body>
</html>
